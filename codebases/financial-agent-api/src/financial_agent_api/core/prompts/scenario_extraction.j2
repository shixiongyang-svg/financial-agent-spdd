Messages:
{% for m in messages -%}
- {{ m.role }}: {{ m.content }}
{% endfor %}

Allowed values:
{% for p, issues in allowed_product_issue_map.items() -%}
- {{ p }}: {{ issues }}
{% endfor %}

Instruction:
You are given the conversation and the allowed product→issues map above.
Select only values that are supported by the allowed list.

Output requirements:
- Return EXACTLY one JSON object.
- product_type must be a string or null.
- issue_type must be a string or null.
- confidence must be a number between 0 and 1.

Answer (ONLY JSON, no markdown, no code blocks):
{
"product_type": {{ product_type | default(None) | tojson }},
"issue_type": {{ issue_type | default(None) | tojson }},
"confidence": {{ confidence | default(0.0) }}
}
