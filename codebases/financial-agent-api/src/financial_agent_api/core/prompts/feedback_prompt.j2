Generate a friendly, specific clarification prompt asking the user for missing information.

List the missing fields and, when allowed_values provided, offer quick-choice suggestions.

User query:
{{ user_query }}

Missing fields: {{ missing_fields }}

Allowed values (optional):
{{ allowed_values }}

Clarifying prompt to user:
